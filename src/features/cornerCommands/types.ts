/** Re-exported, not redeclared -- see oddOneOut/types.ts's identical note;
 * workoutTemplates/types.ts is the actual owner of
 * AssaultBikeConfig.difficulty. */
export type { Difficulty } from "../workoutTemplates/types";

export type RandomFn = () => number;
