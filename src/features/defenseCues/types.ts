/**
 * Fixed, non-user-editable set (PROJECT_FACTS.md) -- same pattern as the
 * bell/clapper SoundAssets, not a CRUD feature parallel to Punches.
 */
export type DefenseCueName = "roll" | "slip" | "duck" | "pivot" | "check" | "clinch";

export type RandomFn = () => number;
